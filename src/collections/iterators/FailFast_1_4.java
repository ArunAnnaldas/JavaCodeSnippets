package collections.iterators;

public class FailFast_1_4 {

	/**
	 * If Iterator detects any structural change after iteration has begun e.g
	 * adding or removing a new element then it throws
	 * ConcurrentModificationException, this is known as fail-fast behavior and
	 * these iterators are called fail-fast iterator because they fail as soon as
	 * they detect any modification
	 * 
	 * Vector, ArrayList, HashMap, HashSet has fail-fast iterators
	 * 
	 */
}
